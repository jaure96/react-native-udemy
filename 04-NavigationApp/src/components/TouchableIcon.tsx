import React from 'react'
import { TouchableOpacity } from 'react-native';

const TouchableIcon = () => {
    return (
        <TouchableOpacity>
            
        </TouchableOpacity>
    )
}

export default TouchableIcon
